# tests
Testes automatizados, espelhando as caixas de `src/agente/`.

Regra: todo cálculo financeiro precisa de ao menos um teste com valor conferido à mão.

**Estado:** `test_schemas.py` (regras de `Declared`, integridade de referências, trecho do case principal representado no perfil).
